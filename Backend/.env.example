# The PHP API (index.php) does NOT load this file.
# All PHP settings are hardcoded in Backend/config.php (and PDO uses DB_* from there via database.php).
#
# Optional: Node scripts (database.ts) use hardcoded credentials in database.ts.
#
# Copy nothing required — this file is documentation only.
